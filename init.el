;;;; SeyZ - Sandro Munda's Emacs Configuration
;;;; <munda.sandro@gmail.com>

(set-language-environment "UTF-8")

;; ----------------------------------------------------------------------------
;; Entry path
;; ----------------------------------------------------------------------------
(add-to-list 'load-path user-emacs-directory)

;; ----------------------------------------------------------------------------
;; Loading globals
;; ----------------------------------------------------------------------------
(load-library "seyz-globals")

;; ----------------------------------------------------------------------------
;; Loading path
;; ----------------------------------------------------------------------------
(load-library "seyz-path")

;; ----------------------------------------------------------------------------
;; Loading gui
;; ----------------------------------------------------------------------------
(load-library "seyz-gui")

;; ----------------------------------------------------------------------------
;; Loading modes
;; ----------------------------------------------------------------------------
(load-library "seyz-mode")

;; ----------------------------------------------------------------------------
;; Loading languages
;; ----------------------------------------------------------------------------
(load-library "seyz-lang")

;; ----------------------------------------------------------------------------
;; Loading custom functions
;; ----------------------------------------------------------------------------
(load-library "seyz-function")

;; ----------------------------------------------------------------------------
;; Loading global shortcuts
;; ----------------------------------------------------------------------------
(load-library "seyz-shortcut")

(setq default-directory "~/")
